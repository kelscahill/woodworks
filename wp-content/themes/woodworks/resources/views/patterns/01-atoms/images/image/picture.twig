{% if image.src %}
  <picture class="o-picture{{ picture.classes ? ' ' ~ picture.classes }}">
    {% if image.sources %}
      {% for source in image.sources %}
        {{ source }}
      {% endfor %}
    {% endif %}
    {% include "@atoms/images/image.twig" with image %}
  </picture>
{% endif %}
