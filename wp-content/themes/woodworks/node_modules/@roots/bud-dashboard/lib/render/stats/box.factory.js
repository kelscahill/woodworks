import chalk from 'chalk';
import { theme } from './theme.js';
export const make = (title, content, options) => `${chalk.hex(options?.color ?? theme.foregroundColor)(title)}\n${content}`;
//# sourceMappingURL=box.factory.js.map