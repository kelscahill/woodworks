import chalk from 'chalk';
import * as box from './box.factory.js';
import { theme } from './theme.js';
export const make = ({ message, file }, color) => box.make(file ?? 'error', message, { color });
export const makeWarning = ({ message, file, type }) => `${chalk.bgHex(theme.foregroundColor).hex(theme.yellow)(type ?? ' WARNING ')} ${file}\n\n${message}\n\n`;
export const makeError = ({ message, file, type }) => `${chalk
    .bgHex(theme.red)
    .white(type ?? ' ERROR ')} ${file}\n\n${message}\n\n`;
export const mapMessages = (messages, color) => messages.map(message => make(message, color));
//# sourceMappingURL=webpack.message.js.map