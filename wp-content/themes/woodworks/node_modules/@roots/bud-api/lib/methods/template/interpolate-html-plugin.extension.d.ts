import { Extension } from '@roots/bud-framework/extension';
import { InterpolateHtmlPlugin } from './interpolate-html-plugin.plugin.js';
/**
 * BudInterpolateHTMLPlugin
 *
 * @public
 * @decorator `@label`
 */
export default class BudInterpolateHtmlPlugin extends Extension<Record<string, RegExp>, InterpolateHtmlPlugin> {
    /**
     * public env accessor
     *
     * @public
     */
    get publicEnv(): Record<string, any>;
    /**
     * `afterConfig` callback
     *
     * @public
     * @decorator `@bind`
     */
    afterConfig(): Promise<void>;
    /**
     * `make` callback
     *
     * @public
     * @decorator `@bind`
     */
    make(): Promise<InterpolateHtmlPlugin>;
    /**
     * `when` callback
     *
     * @public
     * @decorator `@bind`
     */
    when(): Promise<boolean>;
}
//# sourceMappingURL=interpolate-html-plugin.extension.d.ts.map