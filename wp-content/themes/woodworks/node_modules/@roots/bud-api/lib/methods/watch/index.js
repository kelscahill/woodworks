export const watch = function (...input) {
    const app = this;
    app.hooks.on('dev.watch.files', files => input.flat().reduce((files, file) => files.add(file), files));
    return app;
};
//# sourceMappingURL=index.js.map