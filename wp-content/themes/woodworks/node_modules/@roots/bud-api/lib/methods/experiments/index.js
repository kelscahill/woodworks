export const experiments = function (input) {
    const app = this;
    Object.entries(input).map(([k, v]) => app.hooks.on(`build.experiments.${k}`, v));
    return app;
};
//# sourceMappingURL=index.js.map