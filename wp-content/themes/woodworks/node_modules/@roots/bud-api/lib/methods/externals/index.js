export const externals = function (externals) {
    this;
    this.hooks.on('build.externals', (existant) => ({
        ...existant,
        ...externals,
    }));
    return this;
};
//# sourceMappingURL=index.js.map