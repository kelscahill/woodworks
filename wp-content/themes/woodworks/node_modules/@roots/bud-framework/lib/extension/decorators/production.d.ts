/**
 * Extension to be run in `production` mode
 *
 * @remarks
 * Class decorator.
 *
 * Use for plugins and extensions which are chonky and not strictly
 * required for the application to function (minimizers, etc.)
 *
 * @public
 */
export declare const production: <Type extends new (...args: any[]) => any>(constructor: Type) => {
    new (...args: any[]): {
        [x: string]: any;
        /**
         * `when` callback
         *
         * @returns `true` when mode is `production`
         * @public
         */
        when({ isProduction }: {
            isProduction: any;
        }): any;
    };
} & Type;
//# sourceMappingURL=production.d.ts.map