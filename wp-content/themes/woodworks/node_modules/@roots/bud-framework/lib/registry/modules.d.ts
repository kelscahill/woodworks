import type { Extension } from '../extension/index.js';
export interface Modules {
    [key: string]: Extension<any, any>;
}
export declare type ModuleMap = Modules & {
    [K in keyof Modules as `${K & string}`]: Modules[K];
};
export declare type Definitions = {
    [K in keyof ModuleMap as `${K & string}`]?: new (...args: any[]) => ModuleMap[K];
};
//# sourceMappingURL=modules.d.ts.map