/// <reference types="node" />
import type { Config } from '@roots/bud-framework';
import type { BaseContext } from 'clipanion';
import type { Readable, Writable } from 'node:stream';
import type { Application } from './application.js';
import type { Disk } from './disk.js';
import type { Env } from './env.js';
export declare class Context implements Config.Context, BaseContext {
    name: string;
    dir: string;
    manifest: Record<string, any>;
    disk: Disk;
    application: Application;
    env: Env;
    stdin: Readable;
    stdout: Writable;
    stderr: Writable;
    colorDepth: number;
    args: Record<string, string | boolean | undefined | number>;
    constructor(name: string, dir: string, manifest: Record<string, any>, disk: Disk, application: Application, env: Env, stdin?: Readable, stdout?: Writable, stderr?: Writable, colorDepth?: number);
}
//# sourceMappingURL=context.d.ts.map