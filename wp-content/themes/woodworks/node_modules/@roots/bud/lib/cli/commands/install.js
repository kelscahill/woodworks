import chalk from 'chalk';
import { BaseCommand } from './base.js';
/**
 * `bud install` command
 *
 * @deprecated This command was deprecated in 5.3.0
 *
 * @public
 */
export class InstallCommand extends BaseCommand {
    /**
     * Command execute
     *
     * @public
     */
    async execute() {
        this.context.stdout.write(chalk.red(`bud install / bud init is deprecated\n`));
        this.context.stdout.write(`This command was deprecated in 5.3.0. In the future this command will throw an error.\n`);
    }
}
/**
 * Command paths
 *
 * @public
 */
InstallCommand.paths = [[`install`], [`init`]];
//# sourceMappingURL=install.js.map