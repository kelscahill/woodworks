import type { Env as Base } from '@roots/bud-framework';
import { ContainerService } from '@roots/bud-framework';
/**
 * Env service
 *
 * @public
 */
export declare class Env extends ContainerService implements Base.Service {
    /**
     * Bootstrap event callback
     *
     * @public
     * @decorator `@bind`
     */
    bootstrap(): Promise<void>;
    /**
     * Get entries from .env which include `APP_PUBLIC` in their name
     *
     * @public
     * @decorator `@bind`
     * @decorator `@once`
     */
    getPublicEnv(): Record<string, any>;
    /**
     * Transform public env
     *
     * @public
     * @decorator `@bind`
     */
    transformPublicEnv([rawKey, rawValue]: [string, string]): string[];
    /**
     * Filter public env
     *
     * @public
     */
    filterPublicEnv([key]: [string, string]): boolean;
}
//# sourceMappingURL=index.d.ts.map