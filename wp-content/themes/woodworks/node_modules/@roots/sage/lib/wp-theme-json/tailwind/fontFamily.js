export const getFonts = async (path) => {
    const tailwindImport = await import(path);
    const tailwind = tailwindImport?.default ?? tailwindImport;
    return tailwind?.theme?.extend?.fontFamily ?? {};
};
const name = label => `${label.charAt(0).toUpperCase()}${label.slice(1)}`;
export const transform = ([slug, fontFamily]) => ({ name: fontFamily.split(',').shift(), slug, fontFamily });
export const transformFonts = (fonts) => Object.entries(fonts ?? {})
    .map(([k, v]) => [k, Array.isArray(v) ? v.join(' ') : v])
    .map(transform);
//# sourceMappingURL=fontFamily.js.map