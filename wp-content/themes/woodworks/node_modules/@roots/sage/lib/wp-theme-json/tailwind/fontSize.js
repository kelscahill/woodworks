export const getFontSize = async (path) => {
    const tailwindImport = await import(path);
    const tailwind = tailwindImport?.default ?? tailwindImport;
    return tailwind?.theme?.extend?.fontSize ?? {};
};
export const transformEntry = ([slug, fontSize]) => ({ name: slug, slug, size: fontSize });
export const transformFonts = (fonts) => Object.entries(fonts ?? {})
    .map(([k, v]) => [k, Array.isArray(v) ? v.shift() : v])
    .map(transformEntry);
//# sourceMappingURL=fontSize.js.map